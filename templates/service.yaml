apiVersion: v1
kind: Service
metadata:
  name: {{ include "daq-dranspose.fullname" . }}
  labels:
    {{- include "daq-dranspose.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "daq-dranspose.selectorLabels" . | nindent 4 }}
