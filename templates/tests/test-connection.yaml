apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "daq-dranspose.fullname" . }}-test-connection"
  labels:
    {{- include "daq-dranspose.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "daq-dranspose.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
